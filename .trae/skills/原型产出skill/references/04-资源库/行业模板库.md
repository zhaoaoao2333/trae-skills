# 行业模板库

> 行业模板定义各行业的标准页面结构、组件组合和信息层级。
>
> **模板中不包含具体色值**，颜色、字体、间距从 `prototype-rules.md` 的 Design Token 体系获取。
>
> 模板中使用 CSS 变量引用 Token，如 `var(--color-primary)`、`var(--font-title)`。

---

## 电商行业模板

### 模板：首页

```
布局结构（从上到下）：
├─ 顶部搜索区 [固定，高度56px]
│   ├─ 搜索框 [胶囊型，圆角20px，背景var(--color-bg-tertiary)]
│   └─ 消息铃铛 [右侧，24px图标，红点角标]
│
├─ 金刚区 [2行×4列，8个入口]
│   └─ 每个入口：64px图标 + 12px文字 [var(--font-caption)]
│
├─ Banner轮播 [高度160px，圆角12px]
│   └─ 3-5张，自动轮播5s，底部指示点
│
├─ 限时秒杀 [横向滚动]
│   └─ 卡片：倒计时[红色] + 商品图[1:1] + 现价[加粗红色] + 原价[删除线]
│
├─ 分类Tab [横向滚动，6-8个分类]
│   └─ 选中态：品牌色下划线 + 加粗
│
├─ 商品推荐 [双列网格，瀑布流]
│   └─ 商品卡片模板（见下方）
│
└─ 底部导航栏 [4个Tab，固定底部]
```

**商品卡片模板：**
```html
<div class="product-card">
  <img class="card-img" src="[1:1商品图]">
  <div class="card-info">
    <div class="card-title">[标题，2行截断，var(--font-body)]</div>
    <div class="card-price">
      <span class="price-current">[现价，var(--font-title) 加粗 var(--color-primary)]</span>
      <span class="price-original">[原价，var(--font-caption) 删除线 var(--color-text-tertiary)]</span>
    </div>
    <div class="card-tags">
      <span class="tag-primary">[促销标签，品牌色背景白字]</span>
    </div>
    <div class="card-meta">
      <span>[销量，var(--font-caption) var(--color-text-tertiary)]</span>
      <span>[店铺名，var(--font-caption)]</span>
    </div>
  </div>
</div>
```

**隐含功能：**
- 搜索历史、热词推荐
- 秒杀提醒（设置提醒）
- 商品筛选（价格/销量/综合）
- 下拉刷新、上拉加载

---

### 模板：商品列表

```
布局结构：
├─ 顶部导航 [返回 + 标题 + 搜索图标]
├─ 筛选栏 [分类Tab + 排序按钮]
│   └─ 排序：综合/销量/价格 [点击底部弹窗]
├─ 商品列表 [双列网格]
│   └─ 商品卡片模板（同上）
└─ 底部：加载更多/无更多数据
```

**交互：**
- 点击商品 → 商品详情
- 点击购物车图标 → 加入购物车（Toast"已加入购物车"）
- 下拉刷新、上拉加载
- 筛选条件底部弹窗

---

### 模板：商品详情

```
布局结构：
├─ 顶部导航 [返回 + 分享 + 更多]
│   └─ Web/App：右侧有按钮 / 小程序：右侧无按钮（胶囊区）
├─ 商品主图轮播 [1:1，可放大，圆角0]
├─ 价格区
│   ├─ 现价 [var(--font-h1) 加粗 var(--color-primary)]
│   ├─ 原价 [删除线 var(--color-text-tertiary)]
│   └─ 促销标签 [品牌色背景]
├─ 标题区
│   ├─ 商品名称 [var(--font-title)]
│   ├─ 销量 + 评价数 [var(--font-caption)]
│   └─ 分享赚 [按钮]
├─ 规格选择 [点击 → 底部弹窗]
│   └─ 已选：规格名称 [箭头]
├─ 评价区
│   ├─ 评分 [星星 + 分数]
│   ├─ 标签筛选 [好评/有图/追评]
│   └─ 3条评价 [头像+昵称+内容]
├─ 店铺区
│   ├─ 店铺头像 [40px圆形]
│   ├─ 店铺名称 + 评分
│   └─ 进店按钮 [描边按钮]
├─ 详情区 [图文详情，富文本/图片]
└─ 底部固定栏 [固定底部，高度48px]
    ├─ 收藏 [图标按钮]
    ├─ 客服 [图标按钮]
    ├─ 购物车 [图标按钮 + 角标]
    ├─ 加入购物车 [黄色填充按钮]
    └─ 立即购买 [红色填充按钮]
```

**交互：**
- 规格选择 → 底部弹窗 → 选择后更新价格
- 加入购物车 → Toast"已加入购物车" + 购物车角标+1
- 立即购买 → 确认订单页
- 收藏 → 心形图标变色 + Toast

---

## 社区行业模板

### 模板：首页Feed

```
布局结构：
├─ 顶部 [搜索框 + 发布按钮]
├─ 关注/推荐Tab [横向切换，选中态品牌色加粗]
├─ Feed流 [纵向列表]
│   └─ 帖子卡片：
│       ├─ 用户信息 [头像40px圆形 + 昵称 + 时间]
│       ├─ 内容文字 [最多5行，展开"全文"]
│       ├─ 图片网格 [1-9张自适应]
│       │   ├─ 1张：全宽16:9
│       │   ├─ 2-3张：横向等分
│       │   ├─ 4-9张：3列网格
│       ├─ 话题标签 [#话题 蓝色可点击]
│       └─ 互动栏 [点赞 + 评论 + 收藏 + 分享]
│           └─ 点赞动画：心形放大
├─ 底部导航栏 [4个Tab]
└─ FAB [右下角发布按钮，48px圆形，品牌色]
```

**隐含功能：**
- 下拉刷新、上拉加载
- 图片点击放大浏览
- 点赞动画（心形放大）
- 评论展开/收起

---

### 模板：帖子详情

```
布局结构：
├─ 顶部导航 [返回 + "帖子详情" + 更多]
│   └─ 更多 → ActionSheet：举报/拉黑/分享
├─ 帖子内容
│   ├─ 用户信息 [头像 + 昵称 + 关注按钮]
│   ├─ 文字内容
│   ├─ 图片 [可放大浏览]
│   └─ 话题标签
├─ 互动数据
│   └─ 点赞数 + 评论数 + 收藏数 + 时间 [var(--font-caption)]
├─ 评论列表
│   └─ 一级评论：头像 + 昵称 + 内容 + 时间 + 回复按钮
│   └─ 二级评论：缩进 + @用户 + 内容
└─ 底部固定栏
    ├─ 点赞 [图标按钮]
    ├─ 评论输入框 [点击弹出键盘]
    ├─ 收藏 [图标按钮]
    └─ 分享 [图标按钮]
```

**交互：**
- 点击评论 → 弹出键盘回复
- 长按评论 → ActionSheet（复制/举报/删除）
- 点赞 → 动画 + 数字+1

---

### 模板：发布页

```
布局结构：
├─ 顶部导航 [取消 + "发布" + 发布按钮]
│   └─ 发布按钮：品牌色，禁用态灰色
├─ 文本输入区 [大面积输入框]
│   ├─ placeholder："分享你的想法..."
│   └─ 最小高度200px
├─ 图片上传区 [九宫格]
│   ├─ 每张80-100px，1:1，圆角8px
│   ├─ +号占位 [虚线边框]
│   └─ 已上传图片：可删除/排序
├─ 话题选择 [#话题 点击选择或输入]
├─ @用户 [@ 点击选择用户]
└─ 位置 [定位图标 + 位置名称，可选]
```

**交互：**
- 图片上传：点击+号 → 选择图片 → 预览 → 可删除/排序
- 发布按钮：点击后加载动画 → 成功Toast → 跳转首页
- 取消：二次确认"是否保存草稿？"

---

## 本地生活行业模板

### 模板：门店列表

```
布局结构：
├─ 顶部 [搜索框 + 定位城市]
├─ 分类Tab [美食/酒店/景点/娱乐，横向滚动]
├─ 筛选栏 [距离/评分/销量/价格]
│   └─ 点击 → 底部弹窗（多条件组合）
├─ 门店列表 [纵向列表]
│   └─ 门店卡片：
│       ├─ 门店图片 [16:9，圆角8px]
│       ├─ 门店名称 [var(--font-title) Bold]
│       ├─ 评分 [星星 + 分数 + 月售]
│       ├─ 标签 [品类标签 + 优惠标签]
│       ├─ 距离/地址 [var(--font-caption) var(--color-text-tertiary)]
│       └─ 人均/起送 [价格信息]
└─ 底部导航栏
```

**交互：**
- 点击门店 → 门店详情
- 点击导航 → 地图导航（外部地图APP）
- 点击电话 → 拨打电话
- 筛选条件底部弹窗

---

### 模板：门店详情

```
布局结构：
├─ 顶部导航 [返回 + 门店名称 + 收藏/分享]
├─ 门店头图 [16:9轮播]
├─ 基本信息
│   ├─ 名称 [var(--font-h2)]
│   ├─ 评分 + 月售
│   ├─ 营业时间
│   ├─ 地址 [点击 → 地图导航]
│   └─ 电话 [点击 → 拨打电话]
├─ 优惠券 [可领取的优惠券列表]
│   └─ 点击领取 → Toast"领取成功"
├─ 团购/套餐 [横向滚动卡片]
├─ 评价区
│   ├─ 评分分布
│   ├─ 标签筛选
│   └─ 评价列表
├─ 推荐菜/商品 [网格或列表]
└─ 底部固定栏
    ├─ 收藏 [图标按钮]
    ├─ 咨询 [图标按钮]
    └─ 立即购买/下单 [品牌色填充按钮]
```

**交互：**
- 地址 → 点击跳转地图导航
- 电话 → 点击拨打电话
- 优惠券 → 点击领取 → Toast
- 团购 → 点击 → 团购详情 → 购买

---

## 工具类行业模板

### 模板：核心功能页

```
布局结构：
├─ 顶部导航 [返回/菜单 + 标题 + 设置/更多]
├─ 核心操作区 [页面视觉重心]
│   ├─ 主操作按钮 [大尺寸，品牌色，居中]
│   └─ 状态提示 [当前状态/进度]
├─ 数据展示区 [核心数据/结果]
│   └─ 大数字 + 单位 + 趋势
├─ 历史记录 [最近操作列表]
│   └─ 时间 + 内容 + 结果
└─ 底部 [快捷入口/Tab]
```

**隐含功能：**
- 历史记录可查看全部
- 数据可分享/导出
- 设置入口

---

### 模板：历史记录页

```
布局结构：
├─ 顶部导航 [返回 + "历史记录" + 清空]
├─ 筛选栏 [时间范围/类型筛选]
├─ 记录列表 [纵向列表]
│   └─ 记录项：
│       ├─ 时间 [var(--font-caption)]
│       ├─ 内容标题 [var(--font-body)]
│       ├─ 结果/状态 [标签]
│       └─ 操作 [删除/查看]
└─ 空状态 [无记录时引导]
```

---

## 社交行业模板

### 模板：动态信息流页

```
布局结构：
├─ 顶部导航 [返回/头像 + 标题 + 发布按钮]
├─ 发布入口 [快速发布栏，点击展开]
│   ├─ 文字/图片/视频/语音 入口
│   └─ 占位提示 "分享新鲜事..."
├─ 动态列表 [纵向，每条动态一个卡片]
│   └─ 动态卡片模板：
│       ├─ 头部：头像[40px圆形] + 昵称 + 发布时间
│       ├─ 内容区：文字[最多6行，展开收起] + 图片网格[1/2/3/4/6/9张]
│       ├─ 互动区：点赞[图标+数量] + 评论[图标+数量] + 分享
│       └─ 评论区：前2条评论 + "查看更多"
└─ 底部导航 [4个Tab]
```

**隐含功能：**
- 点赞有动画反馈
- 评论支持回复
- 图片支持点击放大浏览
- 长按动态出现操作菜单（举报/不感兴趣/屏蔽）

---

### 模板：聊天会话页

```
布局结构：
├─ 顶部导航 [返回 + 对方头像/昵称 + 更多操作]
├─ 消息列表 [纵向，气泡对话]
│   └─ 消息气泡模板：
│       ├─ 时间分隔线 [间隔>5分钟显示]
│       ├─ 对方消息 [左侧，白色气泡，头像]
│       └─ 我的消息 [右侧，品牌色气泡]
├─ 输入区 [固定底部]
│   ├─ 语音/键盘切换
│   ├─ 输入框 [多行，随内容增高]
│   ├─ 表情按钮
│   └─ 发送按钮 [品牌色，有内容时高亮]
└─ 扩展面板 [点击+展开]
    ├─ 图片/拍照/文件/位置/语音通话/视频通话
```

---

## 企业服务行业模板

### 模板：仪表盘首页

```
布局结构：
├─ 顶部导航 [Logo/品牌 + 消息通知 + 个人中心]
├─ 数据概览区 [横向卡片，3-4个核心指标]
│   └─ 指标卡片：指标名 + 大数字 + 环比趋势[红绿箭头]
├─ 快捷入口 [金刚区，4-6个核心功能]
├─ 待办事项 [列表，高优先级置顶]
│   └─ 待办项：类型标签 + 内容 + 截止时间 + 操作按钮
├─ 数据图表 [折线图/柱状图，最近7天/30天]
└─ 底部导航 [4个Tab]
```

**隐含功能：**
- 数据卡片支持点击下钻
- 待办支持批量处理
- 图表支持切换时间维度
- 支持自定义仪表盘布局

---

### 模板：审批流程页

```
布局结构：
├─ 顶部导航 [返回 + "审批详情" + 更多]
├─ 审批状态条 [流程节点可视化]
│   └─ 提交→部门经理→财务→总经理→完成 [当前节点高亮]
├─ 申请信息 [表单只读展示]
│   └─ 申请人/类型/金额/事由/附件
├─ 审批记录 [时间轴]
│   └─ 审批人 + 意见 + 时间 + 结果[通过/驳回]
└─ 底部操作区 [固定底部]
    ├─ 通过按钮 [品牌色]
    ├─ 驳回按钮 [灰色描边]
    └─ 转交按钮 [文字按钮]
```

---

## 金融行业模板

### 模板：理财首页

```
布局结构：
├─ 顶部导航 [Logo + 客服 + 消息]
├─ 资产总览 [卡片，总资产的视觉化展示]
│   ├─ 总资产金额 [大数字，默认隐藏*号]
│   ├─ 昨日收益 [绿色正数/红色负数]
│   └─ 累计收益
├─ 功能入口 [金刚区，2行×4列]
│   └─ 充值/提现/转账/理财/基金/保险/信用卡/更多
├─ 推荐产品 [横向滚动卡片]
│   └─ 产品卡片：产品名 + 收益率[大红色数字] + 期限 + 起购金额
├─ 热门活动 [Banner轮播]
└─ 底部导航 [4个Tab：首页/理财/生活/我的]
```

**合规要求：**
- 收益率必须标注"业绩比较基准"或"历史收益"
- 产品必须标注风险等级
- 禁止承诺保本保息
- 必须有"理财有风险，投资需谨慎"提示

**隐含功能：**
- 资产支持切换查看不同账户
- 收益支持查看收益明细
- 产品支持对比
- 支持风险测评入口

---

### 模板：交易记录页

```
布局结构：
├─ 顶部导航 [返回 + "交易记录" + 筛选]
├─ 月份筛选 [横向滚动，近12个月]
├─ 收支汇总 [当月收入/支出/结余]
├─ 交易列表 [按日期分组]
│   └─ 交易项：
│       ├─ 图标[交易类型]
│       ├─ 交易名称 + 时间
│       ├─ 交易状态[成功/处理中/失败]
│       └─ 金额[收入红色+/支出绿色-]
└─ 空状态 [无记录时]
```

---

## 教育行业模板

### 模板：课程学习页

```
布局结构：
├─ 顶部导航 [返回 + 课程标题 + 更多]
├─ 视频播放区 [16:9，顶部固定]
│   ├─ 播放/暂停/进度条/倍速/全屏
│   └─ 课程目录浮层 [点击展开]
├─ 课程信息 [视频下方]
│   ├─ 课程名 + 讲师 + 评分
│   ├─ 学习进度条 [百分比+已学课时]
│   └─ 继续学习按钮 [品牌色]
├─ Tab切换 [课程目录/课程详情/学员评价]
│   ├─ 目录Tab：章节列表[锁/已完成/进行中]
│   ├─ 详情Tab：课程介绍/讲师介绍/适合人群
│   └─ 评价Tab：评分统计 + 评价列表
└─ 底部操作区 [固定]
    ├─ 价格/已购状态
    ├─ 加入学习按钮
    └─ 咨询客服
```

**隐含功能：**
- 视频支持断点续播
- 支持笔记功能（时间戳关联）
- 支持倍速播放（0.5x-2x）
- 支持离线下载
- 完成课程后显示证书/成就

---

### 模板：作业/考试页

```
布局结构：
├─ 顶部导航 [返回 + "作业标题" + 倒计时]
├─ 进度条 [题号导航，已完成/当前/未做]
├─ 题目区 [单题展示]
│   ├─ 题号 + 题型[单选/多选/填空/主观]
│   ├─ 题目内容
│   ├─ 选项区 [单选圆形/多选方形]
│   └─ 答题区 [填空输入框/主观文本域]
├─ 操作区 [固定底部]
│   ├─ 上一题/下一题
│   ├─ 标记本题 [收藏/标记不确定]
│   └─ 交卷按钮 [品牌色，最后一题时高亮]
└─ 答题卡浮层 [点击展开，显示所有题号状态]
```

**隐含功能：**
- 支持暂存，下次继续
- 倒计时结束前5分钟提醒
- 支持题目标记，快速跳转
- 交卷前检查未答题

---

## 模板使用说明

### 生成时引用方式

1. **确定行业类型**（电商/社区/本地生活/工具）
2. **选择对应模板**（首页/列表/详情/发布等）
3. **填充业务内容**（替换占位符为具体业务信息）
4. **从Token取样式**（颜色/字体/间距使用CSS变量）
5. **根据PRD调整**（增删模块，调整优先级）

### 模板与Token的关系

```css
/* 模板中使用CSS变量引用Token */
.product-card {
  padding: var(--spacing-card);
  border-radius: var(--radius-card);
  background: var(--color-bg-primary);
}

.card-title {
  font-size: var(--font-body);
  color: var(--color-text-primary);
}

.price-current {
  font-size: var(--font-title);
  font-weight: bold;
  color: var(--color-primary);
}
```

---

## 游戏行业模板

### 模板：游戏首页（游戏大厅）

```
布局结构（从上到下）：
├─ 顶部资产栏 [固定，高度52px]
│   ├─ 头像 [左侧，32px圆形，点击→个人主页]
│   ├─ 资产胶囊组 [Gold + Diamond，居左排列]
│   └─ 通知铃铛 [右侧，红点角标]
│
├─ Banner轮播 [高度140px，圆角16px]
│   └─ 3-5张，自动轮播5s，紫色/金色渐变背景
│
├─ 游戏列表（金刚区） [4列网格]
│   └─ 游戏卡片：图标32px + 名称11px + 角标(HOT/NEW)
│
├─ 快速匹配区 [横向卡片]
│   └─ Quick Play按钮：脉冲发光动画
│
├─ 热门赛事 [横向滚动]
│   └─ 锦标赛卡片：标题+奖金+人数+倒计时
│
└─ 底部导航栏 [5个Tab：游戏/观战/社群/活动/更多]
```

**隐含功能：**
- 游戏卡片Hover：上浮2px+金色边框+发光
- 匹配等待：脉冲动画+预计时间+取消按钮
- 下拉刷新、上拉加载

---

### 模板：游戏对局结算页

```
布局结构：
├─ 结算标题 [胜利/失败，大字+动画]
├─ 段位变化 [段位徽章+升降箭头+动画]
├─ 奖励区 [奖励图标飞入动画]
│   ├─ 金币奖励
│   ├─ 经验值
│   └─ 额外奖励（如有）
├─ 数据统计 [简洁卡片]
│   └─ 本局数据（得分/用时等）
└─ 操作按钮 [再玩一局（主按钮）+ 返回大厅（次按钮）]
```

**交互：**
- 结算动画依次播放：标题→段位→奖励飞入
- "再玩一局"直接进入匹配
- "返回大厅"跳转游戏首页

---

### 模板：个人主页（游戏版）

```
布局结构：
├─ 顶部信息区 [渐变背景]
│   ├─ 头像 [80px圆形+金色光环]
│   ├─ 昵称 + 段位徽章
│   └─ ID + 编辑按钮
├─ 数据卡片 [3列网格]
│   ├─ 总场次
│   ├─ 胜率
│   └─ 段位积分
├─ 资产区 [横向胶囊]
│   ├─ Gold余额
│   └─ Diamond余额
├─ 功能列表
│   ├─ 游戏记录
│   ├─ 好友列表
│   ├─ 战队
│   ├─ 装饰/成就
│   ├─ 充值中心
│   ├─ 商城
│   └─ 设置
└─ 底部导航栏
```
