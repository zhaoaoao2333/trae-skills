# 原型生成样式规范（Jam3a项目适用）

## 一、概述

### 1.1 两种原型形式的定义
- **平铺版（Tile Layout）**：所有页面同时展开展示，适合整体结构浏览、页面关系梳理、交付给开发理解全局
- **手机版（Mobile Simulation）**：模拟真实手机交互，单页面显示，适合体验流程、演示操作、用户测试

### 1.2 适用场景对比

| 场景 | 平铺版 | 手机版 |
|------|--------|--------|
| 需求评审 | 适合，一眼看全所有页面 | 不适合，需逐页切换 |
| 开发阅读 | 适合，快速定位目标页面 | 一般，需配合页面目录 |
| 交互演示 | 不适合，无页面切换效果 | 适合，完整模拟真实体验 |
| 用户测试 | 不适合，非真实使用场景 | 适合，接近真实App体验 |
| 设计走查 | 适合，对比多页面一致性 | 适合，检验单页面细节 |

### 1.3 版本命名规则
- **主版本**：V1、V2、V3...（重大结构调整）
- **子版本**：V3.1（平铺版）、V3.2（手机版）
- **迭代版本**：V3.2.1（小修复）、V3.2.2（功能调整）

---

## 二、平铺版生成规范

### 2.1 布局结构
```
┌─────────────────────────────────────────────────────────────┐
│  页面1(375×812)  │  页面2(375×812)  │  页面3(375×812)  │ ...  │
├──────────────────┼──────────────────┼──────────────────┼──────┤
│  页面5(375×812)  │  页面6(375×812)  │  页面7(375×812)  │ ...  │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 CSS核心规则
```css
/* 页面容器 - 强制全部显示 */
.page {
  display: flex !important;      /* 覆盖默认的display:none */
  position: relative;             /* 不再是fixed定位 */
  width: 375px;                   /* 标准手机宽度 */
  height: 812px;                  /* 标准手机高度 */
  margin: 20px;                   /* 页面间距 */
  border-radius: 20px;            /* 模拟手机屏幕圆角 */
  overflow: hidden;               /* 内容裁剪 */
  box-shadow: 0 8px 32px rgba(0,0,0,0.3);
}

/* 布局容器 */
#app {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  padding: 40px;
  background: #0a0a1a;
}

/* 页面标题 */
.page-title {
  position: absolute;
  top: -30px;
  left: 0;
  font-size: 14px;
  color: #8b5cf6;
}
```

### 2.3 页面标识规范
每个页面必须包含可视化标识：
- **页面序号**：左上角，格式"01"、"02"
- **页面名称**：序号下方，中文名称
- **页面ID**：可选，用于开发对应，如"page-home"
- **视觉区分**：相邻页面使用细边框或阴影区分

### 2.4 内容显示规则
- **强制显示**：所有`.page`元素必须可见，不可隐藏
- **滚动区域**：页面内滚动需正常生效，模拟真实手机滚动
- **固定元素**：顶部导航、底部Tab Bar保持在页面内固定位置
- **弹窗处理**：弹层/Toast默认不显示，或显示在对应页面内（非全局）

### 2.5 交互限制（平铺版特有）
- **无页面切换**：点击跳转类操作不做页面切换
- **Toast提示**：可显示，但只在对应页面区域内
- **弹窗/模态框**：限制在对应页面容器内，禁止全屏遮罩
- **页面导航**：提供页面目录侧边栏，点击后滚动到对应页面位置

### 2.6 目录导航实现
```html
<!-- 固定页面目录面板 -->
<div id="pageIndexPanel">
  <div class="pi-header">
    <span>📋 页面导航</span>
    <span class="pi-close" onclick="togglePageIndex()">✕</span>
  </div>
  <div class="pi-item" onclick="scrollToPage('onboarding')">01. 引导页</div>
  <div class="pi-item" onclick="scrollToPage('register')">02. 注册</div>
  <!-- 更多页面... -->
</div>
```

```javascript
// 平滑滚动到对应页面
function scrollToPage(pageId) {
  const page = document.getElementById(pageId);
  if (page) {
    page.scrollIntoView({ behavior: 'smooth', block: 'center' });
    // 高亮闪烁提示
    page.style.boxShadow = '0 0 30px #d4af37';
    setTimeout(() => {
      page.style.boxShadow = '';
    }, 1000);
  }
}
```

---

## 三、手机版生成规范

### 3.1 布局结构
```
┌──────────────────────────────────────────┐
│  📱 手机外框（可选）                      │
│  ┌──────────────────────────────────────┐│
│  │  顶部状态栏（时间、电量）             ││
│  ├──────────────────────────────────────┤│
│  │                                      ││
│  │           当前激活页面                ││
│  │         （全屏375×812）               ││
│  │                                      ││
│  ├──────────────────────────────────────┤│
│  │  底部导航栏（首页/游戏/我的）         ││
│  └──────────────────────────────────────┘│
└──────────────────────────────────────────┘
     ↕ 其他页面隐藏，通过交互切换
```

### 3.2 CSS核心规则
```css
/* 页面基础样式 */
.page {
  display: none;                  /* 默认隐藏 */
  position: fixed;                /* 全屏定位 */
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: #0a0a1a;
  overflow: hidden;
}

/* 激活页面 */
.page.active {
  display: flex;
  flex-direction: column;
}

/* 页面内容滚动区 */
.page-scroll {
  flex: 1;
  overflow-y: auto;
  overflow-x: hidden;
  -webkit-overflow-scrolling: touch;
}

/* 底部导航栏 */
.bottom-nav {
  height: 56px;
  background: #121228;
  border-top: 1px solid #2d2d44;
  display: flex;
  justify-content: space-around;
  align-items: center;
  padding-bottom: env(safe-area-inset-bottom);
}
```

### 3.3 页面切换机制
```javascript
// 页面显示函数
function showPage(pageId) {
  // 1. 隐藏所有页面
  document.querySelectorAll('.page').forEach(p => {
    p.classList.remove('active');
  });
  
  // 2. 显示目标页面
  const target = document.getElementById(pageId);
  if (target) {
    target.classList.add('active');
    
    // 3. 滚动到顶部
    const scrollArea = target.querySelector('.page-scroll');
    if (scrollArea) scrollArea.scrollTop = 0;
  }
  
  // 4. 更新底部导航选中态
  updateNavActiveState(pageId);
}

// 返回上一页
function goBack() {
  if (pageHistory.length > 1) {
    pageHistory.pop(); // 移除当前页
    const prevPage = pageHistory[pageHistory.length - 1];
    showPage(prevPage);
  }
}
```

### 3.4 导航实现规范

#### 3.4.1 底部Tab Bar
- **固定位置**：始终显示在首页、游戏、商城等一级页面
- **隐藏场景**：二级页面、对局内、全屏页面
- **切换逻辑**：点击切换页面，无返回历史
- **选中态**：图标+文字同时变色

#### 3.4.2 顶部导航栏
- **结构**：返回按钮（左）+ 标题（中）+ 操作按钮（右）
- **返回按钮**：非首页必显，支持逐级返回
- **标题**：当前页面名称，18px Bold

#### 3.4.3 页面目录（侧边栏）
- **触发方式**：悬浮按钮或右上角菜单
- **显示位置**：从右侧滑入
- **内容**：所有页面列表，点击跳转
- **关闭方式**：点击遮罩或关闭按钮

```javascript
// 侧边栏切换
function togglePageIndex() {
  const panel = document.getElementById('pageIndexPanel');
  panel.classList.toggle('show');
}
```

### 3.5 交互闭环要求
每个页面必须完成五问检验：
1. **从哪里来**：是否所有入口都有返回路径？
2. **当前是什么**：页面标题是否清晰？当前位置是否明确？
3. **到哪里去**：核心操作按钮是否醒目？
4. **操作后发生什么**：点击后是否有反馈（Toast/跳转/弹窗）？
5. **异常怎么处理**：网络错误、空数据、权限不足等异常态是否覆盖？

### 3.6 状态覆盖矩阵
每页必须覆盖以下状态：

| 状态 | 必需 | 说明 |
|------|------|------|
| 常规态 | 是 | 正常数据展示 |
| 空状态 | 是 | 无数据时的引导 |
| 加载中 | 是 | 骨架屏或loading |
| 错误态 | 是 | 网络错误、加载失败 |
| 成功态 | 条件 | 有操作后需要 |
| 权限受限 | 条件 | 有权限控制时需要 |

---

## 四、语言切换规范

### 4.1 实现方式
- **方案A：双语内容预置**：HTML中同时包含中英文，通过CSS控制显示/隐藏
- **方案B：JavaScript动态替换**：当前语言内容写入HTML，切换时动态替换文本（推荐）

### 4.2 方案B实现示例
```javascript
// 翻译映射表
const ZH_TO_EN_MAP = {
  "海湾第一游戏平台": "GULF'S #1 GAMING HUB",
  "玩。竞技。赢。": "Play. Compete. Win.",
  // ... 更多词条
};

// 语言切换函数
function switchLanguage(lang) {
  if (lang === currentLang) return;
  
  const isToEn = lang === 'en';
  const map = isToEn ? ZH_TO_EN_MAP : EN_TO_ZH_MAP;
  
  // 遍历所有文本节点进行替换
  const walker = document.createTreeWalker(
    document.body,
    NodeFilter.SHOW_TEXT,
    null,
    false
  );
  
  let node;
  while (node = walker.nextNode()) {
    let text = node.nodeValue;
    if (!text || text.trim() === '') continue;
    
    // 按长度降序匹配，避免部分匹配
    const keys = Object.keys(map).sort((a, b) => b.length - a.length);
    for (const key of keys) {
      if (text.includes(key)) {
        text = text.split(key).join(map[key]);
      }
    }
    node.nodeValue = text;
  }
  
  // 同步更新placeholder
  document.querySelectorAll('[placeholder]').forEach(el => {
    let ph = el.getAttribute('placeholder');
    const keys = Object.keys(map).sort((a, b) => b.length - a.length);
    for (const key of keys) {
      if (ph.includes(key)) {
        ph = ph.split(key).join(map[key]);
      }
    }
    el.setAttribute('placeholder', ph);
  });
  
  currentLang = lang;
  document.documentElement.lang = lang === 'zh' ? 'zh-CN' : 'en';
}
```

### 4.3 切换入口位置
- **设置页**：设置 > 通用 > 语言设置（主要入口）
- **首页**：可选，首页右上角快捷入口
- **引导页**：首次启动时的语言选择弹窗
- **排除位置**：平铺版不显示语言切换（因所有页面同时展示，切换成本过高）

---

## 五、生成脚本规范

### 5.1 脚本职责分离
```
generate_v3.py           # 主脚本，生成V3.1和V3.2
├── extract_pages()      # 从V1提取页面内容
├── generate_tile()      # 生成平铺版
├── generate_mobile()    # 生成手机版
└── translate_content()  # 中文内容翻译保护

add_lang_switch.py       # 添加语言切换功能
fix_v3.py               # 修复翻译错误
```

### 5.2 翻译保护机制
翻译时必须保护以下内容不被翻译：
- HTML标签名和属性名（如`class`、`id`、`style`）
- CSS属性名和值关键字（如`display`、`flex`、`center`）
- JavaScript方法名和关键字（如`function`、`querySelectorAll`）
- 颜色代码（如`#0A0A1A`、`rgba(0,0,0,0.5)`）
- 页面ID和class名（如`page-home`、`active`）

### 5.3 关键保护代码
```python
def process_html_content(html):
    # Step 1: 保护style和script块
    protected_blocks = []
    
    def protect_block(match):
        protected_blocks.append(match.group(0))
        return f'\x00BLOCK{len(protected_blocks)-1}\x00'
    
    html = re.sub(r'<style[^>]*>.*?</style>', protect_block, html, flags=re.DOTALL)
    html = re.sub(r'<script[^>]*>.*?</script>', protect_block, html, flags=re.DOTALL)
    
    # Step 2: 分割HTML标签和文本内容
    parts = re.split(r'(<[^>]+>)', html)
    translated_parts = []
    
    for part in parts:
        if part.startswith('<') and part.endswith('>'):
            # HTML标签 - 只翻译特定属性值
            def translate_attr(m):
                attr_name = m.group(1)
                attr_val = m.group(3)
                if attr_name in ['placeholder', 'value', 'title']:
                    return f'{attr_name}={m.group(2)}{translate_text(attr_val)}{m.group(2)}'
                return m.group(0)
            
            part = re.sub(r'((?:placeholder|value|title)=(["\']))(.*?)(\2)', translate_attr, part)
            translated_parts.append(part)
        elif part.startswith('\x00BLOCK'):
            translated_parts.append(part)
        else:
            # 文本内容 - 翻译
            translated_parts.append(translate_text(part))
    
    html = ''.join(translated_parts)
    
    # Step 3: 恢复保护块
    for i, block in enumerate(protected_blocks):
        html = html.replace(f'\x00BLOCK{i}\x00', block)
    
    return html
```

### 5.4 页面目录翻译
所有页面在目录导航中必须使用中文名称：

| 序号 | 页面ID | 中文名称 |
|------|--------|----------|
| 01 | onboarding | 引导页 |
| 02 | register | 注册 |
| 03 | login | 登录 |
| 04 | profile-setup | 设置资料 |
| 05 | game-home | 游戏首页 |
| ... | ... | ... |

---

## 六、质量检查清单

### 6.1 生成前检查
- [ ] V1基础文件内容完整
- [ ] 所有页面ID正确对应
- [ ] 中文内容已通过翻译保护验证
- [ ] 脚本参数配置正确（版本号、输出路径）

### 6.2 生成后检查
- [ ] 所有页面内容正常显示（无黑屏、无空白）
- [ ] 页面目录中文名称正确
- [ ] 页面跳转链接可达（手机版）
- [ ] 返回路径完整（手机版）
- [ ] 底部导航Tab选中态正确
- [ ] Toast提示文本正确
- [ ] 语言切换功能正常（手机版）
- [ ] 无CSS/JS语法错误
- [ ] 滚动区域正常响应

### 6.3 交付前检查
- [ ] 文件大小合理（< 50MB）
- [ ] 浏览器兼容性测试（Chrome、Safari、Edge）
- [ ] 移动端浏览器可正常打开
- [ ] 页面加载时间 < 5秒

---

## 七、常见问题与解决

### 7.1 平铺版页面黑屏
**原因**：CSS默认隐藏非激活页面（`display:none`）
**解决**：平铺版强制覆盖为`display:flex !important`

### 7.2 翻译破坏代码
**原因**：全文替换导致HTML标签、CSS属性、JS方法名被翻译
**解决**：使用保护机制，先提取style/script块，只翻译标签外的文本内容

### 7.3 页面跳转失效
**原因**：页面ID变更或函数名错误
**解决**：统一使用`showPage(pageId)`函数，避免内联复杂JS

### 7.4 语言切换后布局错乱
**原因**：阿拉伯语文本长度超出原设计空间
**解决**：预留20-30%文本扩展空间，使用弹性布局

---

**适用项目**：P008-Jam3a-2607
**版本**：v1.0
**更新日期**：2026-07-29
**下次评审**：原型V4生成前