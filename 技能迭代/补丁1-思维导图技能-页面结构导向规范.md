# 补丁1：思维导图生成技能 - 页面结构导向规范补充

> **补丁目的**：在现有功能导向思维导图规范基础上，补充页面结构导向的思维导图生成规范。
> **触发条件**：当PM要求"以页面结构为导向"生成思维导图时，使用本规范。
> **参考来源**：`思维导图模板与规范.md` + `Jam3a-思维导图--V5.mm`（实际模板案例）
> **影响范围**：思维导图生成skill（5个文件变更 + 1个新文件）
> **更新记录**：v2 — 追加"不设层级上限"规则（适用于所有导向模式）

---

## 变更概览

| 操作 | 文件路径 | 说明 |
|------|---------|------|
| 新增 | `references/03-规则规范/页面结构导向思维导图规范.md` | 页面结构导向的完整规范 |
| 修改 | `references/02-模板文件/思维导图模板.md` | 追加页面结构导向模板 |
| 修改 | `references/01-Agent定义/思维导图Agent.md` | 追加页面结构导向模式 + 取消层级上限 |
| 修改 | `SKILL.md` | 追加导向模式选择机制 + 取消层级上限 |
| 修改 | `references/03-规则规范/思维导图生成规范.md` | 追加页面结构导向引用说明 + 取消4级层级限制 |
| 修改 | 全部导向模式（通用规则） | 不设层级上限，允许逐层生成；一次性完整版以功能/需求为导向忽略层级限制 |

---

## 一、新增文件

### 文件：`references/03-规则规范/页面结构导向思维导图规范.md`

```markdown
# 页面结构导向思维导图规范

> 本文件定义页面结构导向的思维导图生成规则。
> 当PM要求"以页面结构为导向"生成思维导图时，使用本规范替代功能导向规范中的结构规则。
> 参考模板：Jam3a-思维导图--V5.mm
>
> **与功能导向规范的区别**：
> - 功能导向：端口→模块→功能→子功能，限4级，每节点15字
> - 页面结构导向：端→页面→板块→内容/交互/跳转，不限层级，每节点25字

---

## 一、适用范围

- 移动端App（页面结构主导）
- 管理后台（功能模块主导，但需描述页面级信息时）
- 需要作为原型设计输入源的场景
- 需要梳理完整页面架构和跳转关系的场景

---

## 二、移动端模板（页面结构主导）

### 2.1 结构原则

```
根节点
├── App端
│   ├── 注册登录流程（前置页面）
│   ├── Tab1首页（一级页面）
│   │   ├── 板块A（页面内的视觉区域）
│   │   │   ├── 内容描述
│   │   │   ├── 交互描述
│   │   │   └── → 跳转至：目标页面
│   │   ├── 板块B
│   │   └── 底部TabBar
│   ├── Tab2首页
│   ├── ...
│   ├── 游戏内通用页面（如有）
│   └── 专项流程页面（如有）
```

### 2.2 节点层级规则

| 层级 | 内容 | 示例 |
|------|------|------|
| 第1级 | 端名称 | App端 |
| 第2级 | 页面名称/Tab名称 | 游戏Tab首页 |
| 第3级 | 页面内板块/区域 | 顶部个人资产栏 |
| 第4级 | 板块内容/交互/跳转 | Gold余额显示 |
| 第5级+ | 子页面展开（仅唯一入口时） | 匹配等待页详细内容 |

### 2.3 板块节点写法

**板块有交互/可跳转** → 继续往下展开：
```
游戏列表（金刚区，4列网格卡片）
  - Ludo卡片：封面 + 在线人数 + 快速开始按钮
    - 操作：点击卡片 → 跳转至：匹配等待页
  - Jackaroo卡片（结构同Ludo）
  - 分类Tag筛选（全部/棋牌/休闲/热门）
  - 搜索框（搜索游戏名称）
```

**板块无交互** → 描述内容和逻辑即可：
```
等级XP进度条
  - 显示当前等级和升级进度
  - XP来源：游戏竞技/社交互动/任务/消费行为
  - 每日XP上限可配置
```

### 2.4 唯一入口与跳转引用规则

**核心原则：页面信息只在一处详细定义，其他地方通过精确跳转引用。**

#### 判断标准

| 情况 | 处理方式 |
|------|---------|
| 当前页面是某目标页面的**唯一入口** | 目标页面就地展开（继续往下画子节点） |
| 当前页面**不是唯一入口**（多处可进入） | 只写 `→ 跳转至：XXX页`，不展开 |

#### 跳转引用格式
统一使用：`→ 跳转至：XXX页`

#### 常见"非唯一入口"页面
- 个人主页（头像、好友列表、更多Tab等多处可进）
- 消息中心（顶部消息入口、更多Tab等多处）
- 商城（Diamond入口、更多Tab等）
- 充值中心（Gold入口、VIP页等）
- 设置页（更多Tab等）

这些页面选择一个**主入口**首次展开（通常在"更多Tab"首页），其他位置只写引用。

### 2.5 交互效果描述规范

**不用强制标签**（如【内容】【作用】），用自然文字描述。

交互效果描述要点：
- 描述用户操作的直接反馈（如"点击后弹出XX"）
- 描述动画/视觉效果（如"卡片翻转""脉冲动画"）
- 描述状态变化（如"按钮变为不可点击""图标高亮"）

示例：
```
Quick Play按钮
  - 快速匹配最近玩过的游戏
  - 点击后进入匹配等待页
  - 匹配动画：雷达搜索脉冲效果
  - 匹配成功：对手信息弹窗 + VS动画 + 屏幕闪白
```

### 2.6 页面状态覆盖

核心页面需覆盖以下状态（在节点中自然描述，不强制标签）：
- 常规态：正常数据展示
- 空状态：无数据时的引导（插图+文案+操作按钮）
- 加载态：骨架屏或加载动画
- 错误态：网络异常/请求失败的提示与重试

---

## 三、管理端模板（功能模块主导）

### 3.1 结构原则

```
根节点
├── 管理端
│   ├── 模块1（一级功能模块）
│   │   ├── 子功能1
│   │   │   ├── 数据字段
│   │   │   ├── 操作说明
│   │   │   └── 业务规则
│   │   ├── 子功能2
│   │   └── ...
│   ├── 模块2
│   └── ...
```

### 3.2 节点层级规则

| 层级 | 内容 | 示例 |
|------|------|------|
| 第1级 | 端名称 | 管理端 |
| 第2级 | 功能模块名称 | 赛事管理 |
| 第3级 | 子功能 | 锦标赛创建/编辑/删除 |
| 第4级 | 数据字段/操作/规则 | 查询条件/操作按钮/校验规则 |
| 第5级+ | 字段明细 | 用户ID/用户名/手机号 |

### 3.3 模块节点写法

每个模块按以下维度自然展开（不用标签，直接描述）：

```
赛事管理
  - 平台赛事全生命周期管理
  - 锦标赛创建/编辑/删除
    - 创建表单：名称/时间/类型/赛制/奖励/报名条件
    - 每游戏独立管理
    - 名称唯一性校验
    - 时间冲突检测
  - 状态管理
    - 状态流转：草稿 → 报名中 → 进行中 → 已结束
    - 草稿→报名中需所有参数完整
    - 进行中→已结束需所有比赛完成
  - 参与者管理
    - 参与者列表：用户信息/报名时间/状态/排名
    - 搜索/筛选/导出
  - 结果管理
    - 结果录入/修改/确认
    - 奖励发放
```

### 3.4 管理端特殊规范

- 参考海外后台风格（左侧导航菜单 + 主内容区）
- 数据字段写明字段名（便于开发对接）
- 业务规则和校验逻辑明确写出
- 权限相关标注可见角色
- 审计操作标注日志记录要求

---

## 四、通用规范

### 4.1 节点文字
- 每个节点文字控制在25字以内
- 超长描述拆分为多个子节点
- 不使用模糊表述（如" etc." "..." "相关功能"）

### 4.2 标注规范

| 标注 | 含义 | 使用场景 |
|------|------|---------|
| [待确认] | 需要PM确认的内容 | 不确定的功能/规则 |
| [AI补充] | AI基于常识补充的节点 | 行业通用模块补充 |
| [MVP] | 首次交付的最小可用范围 | 核心路径功能 |
| [Phase 2] | 后续阶段实现 | 预留功能 |

### 4.3 AI补充限制
- AI补充节点不超过总节点数的20%
- 补充的必须是行业通用模块（如设置、帮助、关于等）
- 不可编造竞品功能或不存在的业务逻辑

### 4.4 层级控制
- 移动端：不限层级，以完整描述为目标
- 管理端：不限层级，以字段级明细为目标
- 同层级节点必须属于同一逻辑范畴，无包含关系

### 4.5 mm文件格式要求

```xml
<?xml version="1.0" encoding="UTF-8"?>
<map version="1.0.1">
  <node TEXT="项目名称 — 思维导图">
    <node TEXT="App端">
      <node TEXT="页面名称">
        <node TEXT="板块名称">
          <node TEXT="内容/交互描述" />
        </node>
      </node>
    </node>
    <node TEXT="管理端">
      ...
    </node>
  </node>
</map>
```

- XML格式正确，可被思维导图工具正常导入
- 使用 `TEXT` 属性存储节点文字
- 不使用 richcontent（保持简洁）
- 编码必须为 UTF-8

---

## 五、质量检查清单

| # | 检查项 | 检查标准 |
|---|--------|---------|
| 1 | 结构完整性 | 一级分支覆盖核心主题全部范围 |
| 2 | 唯一入口规则 | 页面信息只定义一次，非唯一入口只写引用 |
| 3 | 跳转格式统一 | 所有引用使用 `→ 跳转至：XXX页` |
| 4 | 节点文字 | 25字以内，无模糊表述 |
| 5 | 逻辑并列 | 同层级节点属同一逻辑范畴 |
| 6 | 标注规范 | [待确认]/[AI补充]/[MVP] 使用正确 |
| 7 | AI补充占比 | 不超过20% |
| 8 | mm文件可导入 | XML格式正确，UTF-8编码 |
| 9 | 交互描述 | 核心页面有交互效果描述 |
| 10 | 状态覆盖 | 核心页面覆盖常规/空/加载/错误态 |

---

## 六、页面结构导向 vs 功能导向 对照表

| 维度 | 功能导向（原有） | 页面结构导向（本规范） |
|------|----------------|---------------------|
| 适用场景 | 功能清单梳理、业务流程 | 页面架构设计、原型输入源 |
| 一级分支 | 按端口/角色/功能域 | 按端（App端/管理端） |
| 二级分支 | 模块/页面 | 页面名称/Tab名称 |
| 三级分支 | 功能点 | 页面内板块/区域 |
| 四级分支 | 子功能/异常 | 板块内容/交互/跳转 |
| 层级限制 | 最多4级 | 不限（以完整描述为目标） |
| 节点字数 | 15字以内 | 25字以内 |
| 跳转引用 | 无特殊规则 | 唯一入口展开，非唯一入口写引用 |
| 交互描述 | 无特殊要求 | 核心页面需描述交互效果 |
| 状态覆盖 | 无特殊要求 | 核心页面需覆盖4种状态 |
| mm文件编码 | 未指定 | 必须UTF-8 |
```

---

## 二、修改文件

### 2.1 修改 `references/02-模板文件/思维导图模板.md`

在文件末尾（场景模板骨架之后）追加以下内容：

```markdown
---

## 四、页面结构导向模板

> 当PM要求"以页面结构为导向"生成思维导图时，使用以下模板。
> 遵循 `references/03-规则规范/页面结构导向思维导图规范.md` 中的规则。

### 4.1 结构确认模板（页面结构导向）

```
【核心主题】：XXX — 页面架构思维导图

【导向模式】：页面结构导向

【一级分支预览】：
- App端 — 【详细展开】完整页面架构
  - 前置流程：注册登录（X个页面）
  - Tab1首页（一级页面）— 【详细展开】
  - Tab2首页（一级页面）— 【简要处理】
  - ...
- 管理端 — 【详细展开】功能模块架构

【端类型】：App端 + 管理端 / 仅App端 / 仅管理端

以上结构中，标注"详细展开"的页面会展开到板块级（含内容/交互/跳转），标注"简要处理"的页面只列页面名称和核心板块。确认后我将展开完整导图。
```

### 4.2 页面结构导向输出格式（Markdown）

```
# 【项目名】页面架构思维导图

- App端
  - 注册登录流程（前置页面）
    - 引导页
      - 品牌展示区
        - 品牌Logo（居中展示，淡入动画）
        - 品牌标语（多语言文案）
      - 引导内容区
        - 3页滑动引导（核心玩法/社交竞技/锦标赛奖励）
        - 滑动指示器（圆点，当前页高亮）
      - 底部操作区
        - 跳过按钮 → 跳转至：游戏Tab首页（游客模式）
        - 注册按钮 → 跳转至：注册页
    - 注册页
      - ...
  - Tab1首页（一级页面）
    - 顶部资产栏
      - Gold余额显示（点击 → 跳转至：充值中心）
      - Diamond余额显示（点击 → 跳转至：商城）
      - 头像（点击 → 跳转至：个人主页）
    - 游戏列表（金刚区，4列网格）
      - Ludo卡片
        - 封面 + 在线人数 + 快速开始按钮
        - 操作：点击卡片 → 跳转至：匹配等待页
      - Jackaroo卡片（结构同Ludo）
      - 分类Tag筛选
      - 搜索框
    - 底部TabBar
  - Tab2首页
    - → 跳转至：详见Tab2首页展开（如有唯一入口）

- 管理端
  - 赛事管理
    - 锦标赛创建/编辑/删除
      - 创建表单：名称/时间/类型/赛制/奖励/报名条件
      - 名称唯一性校验
      - 时间冲突检测
    - 状态管理
      - 状态流转：草稿 → 报名中 → 进行中 → 已结束
    - 参与者管理
      - 参与者列表：用户信息/报名时间/状态/排名
    - 结果管理
      - 结果录入/修改/确认
      - 奖励发放

AI补充节点X个，待确认节点X个。
```

### 4.3 页面结构导向 .mm 模板

```xml
<?xml version="1.0" encoding="UTF-8"?>
<map version="1.0.1">
  <node TEXT="项目名 — 页面架构思维导图">
    <node TEXT="App端">
      <node TEXT="注册登录流程">
        <node TEXT="引导页">
          <node TEXT="品牌展示区">
            <node TEXT="品牌Logo（居中展示，淡入动画）" />
            <node TEXT="品牌标语（多语言文案）" />
          </node>
          <node TEXT="引导内容区">
            <node TEXT="3页滑动引导" />
            <node TEXT="滑动指示器" />
          </node>
          <node TEXT="底部操作区">
            <node TEXT="跳过按钮 → 跳转至：游戏Tab首页（游客模式）" />
            <node TEXT="注册按钮 → 跳转至：注册页" />
          </node>
        </node>
      </node>
      <node TEXT="Tab1首页">
        <node TEXT="顶部资产栏">
          <node TEXT="Gold余额（点击 → 跳转至：充值中心）" />
          <node TEXT="Diamond余额（点击 → 跳转至：商城）" />
          <node TEXT="头像（点击 → 跳转至：个人主页）" />
        </node>
        <node TEXT="游戏列表（金刚区）">
          <node TEXT="Ludo卡片：封面+在线人数+快速开始" />
          <node TEXT="操作：点击卡片 → 跳转至：匹配等待页" />
        </node>
        <node TEXT="底部TabBar" />
      </node>
    </node>
    <node TEXT="管理端">
      <node TEXT="赛事管理">
        <node TEXT="锦标赛创建/编辑/删除">
          <node TEXT="创建表单：名称/时间/类型/赛制/奖励/报名条件" />
          <node TEXT="名称唯一性校验" />
        </node>
        <node TEXT="状态管理">
          <node TEXT="状态流转：草稿→报名中→进行中→已结束" />
        </node>
      </node>
    </node>
  </node>
</map>
```

### 4.4 场景选择指南

| PM需求 | 导向模式 | 使用规范 |
|--------|---------|---------|
| "梳理产品功能清单" | 功能导向 | 思维导图生成规范.md（原有） |
| "梳理业务流程" | 功能导向 | 思维导图生成规范.md（原有） |
| "梳理页面架构" | 页面结构导向 | 页面结构导向思维导图规范.md（新增） |
| "作为原型设计的输入" | 页面结构导向 | 页面结构导向思维导图规范.md（新增） |
| "梳理页面跳转关系" | 页面结构导向 | 页面结构导向思维导图规范.md（新增） |
| "全端思维导图（App+管理后台）" | 页面结构导向 | 页面结构导向思维导图规范.md（新增） |
| 未明确导向 | 功能导向（默认） | 思维导图生成规范.md（原有） |
```

---

### 2.2 修改 `references/01-Agent定义/思维导图Agent.md`

在"输出场景适配"表格之后、"硬约束"之前，追加以下内容：

```markdown
---

## 导向模式选择

思维导图Agent支持两种导向模式，根据PM需求选择：

### 模式选择时机

在**步骤3.2（AI整理思维导图框架）**时，Agent根据输入材料判断导向模式：

| 输入特征 | 推荐导向 | 判断依据 |
|---------|---------|---------|
| PM提供功能清单/需求列表 | 功能导向 | 梳理功能模块 |
| PM提供PRD/页面描述 | 页面结构导向 | 梳理页面架构 |
| PM要求"作为原型输入" | 页面结构导向 | 需要页面级信息 |
| PM要求"梳理页面跳转" | 页面结构导向 | 需要跳转关系 |
| PM未明确 | 功能导向（默认） | 按默认模式处理 |

### 模式确认

Agent在输出框架时，必须标注所选导向模式：
```
【导向模式】：功能导向 / 页面结构导向
```

如PM未明确要求，Agent根据输入材料推荐模式，但最终由PM确认。

### 页面结构导向模式特殊行为

当选定页面结构导向模式时，Agent执行以下额外行为：

1. **端类型确认**：询问PM需要覆盖哪些端（App端/管理端/全端）
2. **页面清单梳理**：先列出所有页面名称，PM确认后再展开板块
3. **跳转关系梳理**：识别唯一入口页面和非唯一入口页面
4. **交互效果描述**：对核心页面补充交互效果描述
5. **状态覆盖检查**：对核心页面检查4种状态是否覆盖
6. **层级不限**：以完整描述为目标，不受4级限制
7. **节点字数**：每节点25字以内（功能导向为15字）

### 加载规范

| 导向模式 | 加载的规范文件 |
|---------|-------------|
| 功能导向 | `references/03-规则规范/思维导图生成规范.md` |
| 页面结构导向 | `references/03-规则规范/页面结构导向思维导图规范.md` |
```

同时，在"硬约束"部分追加：

```markdown
10. 页面结构导向模式下，必须遵循唯一入口与跳转引用规则
11. 页面结构导向模式下，核心页面必须有交互效果描述和状态覆盖
12. 页面结构导向模式下，mm文件编码必须为UTF-8
```

---

### 2.3 修改 `SKILL.md`

在"引用规则"部分追加：

```markdown
- **页面结构导向思维导图**：
  - 当PM要求以页面结构为导向时，加载 `references/03-规则规范/页面结构导向思维导图规范.md`
  - 加载 `references/02-模板文件/思维导图模板.md` 中的"页面结构导向模板"章节
  - 导向模式在阶段三（需求梳理）步骤3.2时确定，PM确认后生效
```

在"阶段三：需求梳理"的"步骤 3.2"中追加：

```markdown
- **导向模式选择**：根据输入材料判断使用功能导向还是页面结构导向
  - 功能导向（默认）：适用于功能清单梳理、业务流程整理
  - 页面结构导向：适用于页面架构设计、原型设计输入源、页面跳转关系梳理
  - 在框架输出中标注所选导向模式，PM确认后生效
```

在"阶段四：思维导图生成"的"步骤 4.1"中追加：

```markdown
- 如选定页面结构导向模式：
  - 加载 `references/03-规则规范/页面结构导向思维导图规范.md`
  - 确认端类型（App端/管理端/全端）
  - 先输出页面清单，PM确认后展开板块
```

在"步骤 4.3 质量检查"的自检清单中追加：

```markdown
| 11 | 导向模式一致性 | 如为页面结构导向，检查唯一入口规则、跳转格式、交互描述、状态覆盖 |
| 12 | mm文件编码 | 如为页面结构导向，检查XML编码为UTF-8 |
```

---

### 2.4 修改 `references/03-规则规范/思维导图生成规范.md`

在"十、适用范围"之前，追加以下内容：

```markdown
---

## 十一、页面结构导向规范（补充）

当PM要求"以页面结构为导向"生成思维导图时，以下规则**被页面结构导向规范覆盖**：

| 原规则（功能导向） | 页面结构导向规则 | 规范文件 |
|------------------|----------------|---------|
| 层级不超过4级 | 不限层级，以完整描述为目标 | 页面结构导向思维导图规范.md |
| 每节点15字以内 | 每节点25字以内 | 页面结构导向思维导图规范.md |
| 无跳转引用规则 | 唯一入口展开，非唯一入口写引用 | 页面结构导向思维导图规范.md |
| 无交互描述要求 | 核心页面需描述交互效果 | 页面结构导向思维导图规范.md |
| 无状态覆盖要求 | 核心页面需覆盖4种状态 | 页面结构导向思维导图规范.md |
| mm文件无编码要求 | mm文件必须UTF-8编码 | 页面结构导向思维导图规范.md |

**未覆盖的规则继续生效**：核心原则（逻辑一致/结构平衡/详略得当/一键可用）、主题规范、标注规范、AI补充限制、输出流程等。

完整页面结构导向规范见 `references/03-规则规范/页面结构导向思维导图规范.md`。
```

---

### 2.5 修改 `references/03-规则规范/思维导图生成规范.md`（取消层级上限 + 逐层生成模式）

> **此修改适用于所有导向模式（功能导向 + 页面结构导向），是通用规则变更。**

#### 变更1：核心原则第3条

原文：
```
3. **层级克制**：总层级不超过 4 级。超过 4 级说明结构需要重新组织。
```

替换为：
```
3. **不限层级**：不设层级上限，允许逐层生成。以完整描述为目标，该拆分子级节点就拆分子级节点。当PM要求一次性生成完整版导图时，以功能和需求为导向，忽略层级限制，生成完整的思维导图。
```

#### 变更2：四、层级与结构规范 — 层级控制

原文：
```
### 层级控制

- 总层级不超过 4 级（核心主题算第 1 级）
- 详细展开区域：3-4 级
- 简要处理区域：1-2 级，用合并概括或标注"（略）"收住
```

替换为：
```
### 层级控制

- 不设层级上限，以完整描述为目标
- 允许逐层生成：先输出一级框架，PM确认后逐层展开子级节点
- 一次性完整版：当PM要求一次性生成完整版导图时，以功能和需求为导向，该拆分的就拆分，不受层级限制
- 详细展开区域：深入到子级节点直至完整描述
- 简要处理区域：1-2级，用合并概括或标注"（略）"收住
```

#### 变更3：自检清单第1项

原文：
```
| 1 | 层级控制 | 总层级不超过4级 |
```

替换为：
```
| 1 | 层级完整性 | 不设上限，但每个子节点必须属于其父节点的逻辑范畴 |
```

#### 变更4：追加"生成模式"章节

在"九、输出流程"之后追加：

```markdown
---

## 十、生成模式

思维导图支持两种生成模式：

### 逐层生成模式（默认）

- 先输出一级框架，PM确认后逐层展开
- 每层展开后等待PM确认再继续下一层
- 适用于：大型项目、需要逐步确认的场景

### 一次性完整版模式

- 当PM明确要求"一次性生成完整版""直接输出完整导图"时启用
- 以功能和需求为导向，忽略层级限制
- 该拆分子级节点就拆分子级节点，以完整描述为目标
- 输出后附完整说明（AI补充节点数、待确认节点数）
- 适用于：PM已有明确需求、需要快速产出完整导图的场景

### 模式选择规则

| PM指令 | 生成模式 | 行为 |
|--------|---------|------|
| "先看框架" / "逐步展开" | 逐层生成 | 输出一级框架，等待确认 |
| "一次性生成" / "完整版" / "直接输出" | 一次性完整版 | 忽略层级限制，直接输出完整导图 |
| 未明确 | 逐层生成（默认） | 输出一级框架，等待确认 |
```

---

### 2.6 修改 `references/01-Agent定义/思维导图Agent.md`（取消层级上限）

#### 变更1：硬约束第3条

原文：
```
3. 层级不超过 4 级
```

替换为：
```
3. 不设层级上限，以完整描述为目标。逐层生成时每层等待PM确认；一次性完整版时以功能/需求为导向，该拆分就拆分
```

#### 变更2：硬约束第4条（节点字数放宽）

原文：
```
4. 每个节点的文字控制在 15 字以内
```

替换为：
```
4. 每个节点的文字控制在 25 字以内（功能导向15字为建议值，非硬性限制）
```

#### 变更3：工作流程追加生成模式

在"第一步：结构确认（强制）"之前追加：

```markdown
### 生成模式确认

根据PM指令确认生成模式：

| PM指令 | 生成模式 | 行为 |
|--------|---------|------|
| "先看框架" / "逐步展开" / 未明确 | 逐层生成 | 输出一级框架，PM确认后逐层展开 |
| "一次性生成" / "完整版" / "直接输出" | 一次性完整版 | 忽略层级限制，直接输出完整导图 |

**一次性完整版模式特殊行为**：
- 以功能和需求为导向
- 不受层级限制，该拆分子级节点就拆分
- 直接输出完整的.mm和.md文件
- 输出后附说明（AI补充节点数、待确认节点数）
```

---

### 2.7 修改 `SKILL.md`（取消层级上限 + 生成模式）

#### 变更1：步骤4.2展开内容追加

在"步骤 4.2 — 展开内容"中追加：

```markdown
**生成模式**：
- **逐层生成（默认）**：先输出一级框架，PM确认后逐层展开子级节点
- **一次性完整版**：当PM要求"一次性生成完整版""直接输出完整导图"时，以功能和需求为导向，忽略层级限制，该拆分就拆分，直接输出完整导图
```

#### 变更2：步骤4.3自检清单修改

原文：
```
| 1 | 层级控制 | 总层级不超过4级 |
```

替换为：
```
| 1 | 层级完整性 | 不设上限，每个子节点必须属于其父节点的逻辑范畴 |
```

---

## 三、补丁验证清单

更新完成后，请验证以下内容：

- [ ] 新文件 `页面结构导向思维导图规范.md` 已创建，内容完整
- [ ] `思维导图模板.md` 末尾追加了页面结构导向模板（结构确认模板 + MD格式 + MM模板 + 场景选择指南）
- [ ] `思维导图Agent.md` 追加了导向模式选择机制和页面结构导向特殊行为
- [ ] `SKILL.md` 追加了页面结构导向的引用规则、阶段三模式选择、阶段四规范加载、自检项
- [ ] `思维导图生成规范.md` 追加了页面结构导向覆盖说明
- [ ] 所有文件间的交叉引用路径正确
- [ ] 功能导向的原有规则未被修改（仅追加，不覆盖）
